#include <iostream>
using namespace std;
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    int a,n; cin>>a>>n;
    while(n<=0)cin>>n;
    
    int sum=0;
    while(n!=0){
        n--;
        sum+=n+a;
    }
    
    cout<<sum<<endl;
    return 0;
}
